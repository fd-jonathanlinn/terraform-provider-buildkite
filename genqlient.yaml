schema: schema.graphql

operations:
  - buildkite/graphql/*.graphql

generated: buildkite/generated.go

# We pass context.Background() everywhere so just leave it off
context_type: "-"

bindings:
  YAML:
    type: string
  PipelineStepsInput:
    type: map[string]string
  PipnlineTeamAssignmentInput:
    type: map[string]string
  PipelineVisibility:
    type: string
  PipelineTagInput:
    type: map[string]string
